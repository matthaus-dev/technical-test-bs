<IfModule mod_rewrite.c>
    Options -MultiViews
    AcceptPathInfo On
    
    RewriteEngine On
    # se não for arquivo nem diretório, encaminha para index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ index.php [QSA,L]
</IfModule>